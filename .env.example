# Copy this file to `.env` for local development.
#
# These values are safe to expose in the browser (publishable/anon).
VITE_SUPABASE_URL="https://bfxypsjdwdrgedxyiaba.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_wifNwaLO3kjTCSuB7Ix6aw_0hPQmx6C"
# Optional public preview endpoint (relies on CORS + rate limiting, not a client secret):
# VITE_PREVIEW_MODEL_API_ENDPOINT="https://<project>.supabase.co/functions/v1/preview-model"

# Server-side secret for local Supabase Edge Function rate limiting.
# This is NOT exposed to the browser because it does not use the `VITE_` prefix.
RATE_LIMIT_SECRET="replace-me-for-local-edge-runtime"
